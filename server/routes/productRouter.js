const express = require("express");
const router = express.Router();
// const dbConn = require('../server').dbConn;
const products = require('../Models/ProductModel');

router.get("/", (req, res) => {

})

